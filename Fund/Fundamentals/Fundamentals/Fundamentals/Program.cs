

class Sample
{
    static void Main(string[] args)
    {
        /* Starting point of program execution.
         * When code is being compiled, CLR will look for the Main method.
         * Main method should be static. A static method can be executed without creating any object for the class.
         * System.Console.WriteLine();
         * System is the namespace, Console is the class, and WritelLine() is a method inside the 
         * Console class.
        */
        Console.WriteLine("C# Fundamentals\n");

        Console.WriteLine("Welcome");
        Console.WriteLine("to");
        Console.WriteLine("C# Program");
        Console.ReadKey();
    }
}


